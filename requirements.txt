# ── core (พอสำหรับ ingest → geo → graph → 3 retrievers → eval → UI) ──
python-dotenv>=1.0
requests>=2.31
pydantic>=2.6

# graph
neo4j>=5.20

# geo
geopandas>=0.14
shapely>=2.0
pyproj>=3.6

# data / eval / vector baseline (vector-rag ใช้ scikit-learn ไม่ต้องใช้ faiss)
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4

# LLM generation (optional ตอนรัน; retriever ปัจจุบันเป็น deterministic ไม่บังคับ key)
anthropic>=0.40

# UI
streamlit>=1.36
pydeck>=0.9        # overlay flood extent บนแผนที่

# test
pytest>=8.2

# ── heavy/optional (สำหรับต่อยอด — ไม่ได้ใช้ใน pipeline ปัจจุบัน) ──
# ติดตั้งเพิ่มเมื่อจะใช้ PropertyGraphIndex เต็มรูป / FAISS / ragas:
#   pip install -r requirements-optional.txt
