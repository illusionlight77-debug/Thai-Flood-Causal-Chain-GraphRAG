# secrets
.env

# internal working docs — เก็บไว้ในเครื่อง ไม่แสดงบน GitHub
CLAUDE.md
KICKOFF.md

# raw data (keep processed/fixtures if small; ignore heavy raw pulls)
data/raw/*
# ...except committed real inputs needed for reproducibility (Item 3):
!data/raw/gadm41_THA_1.json
*.tif
*.tiff
*.gpkg

# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/

# vector stores / indexes
storage/
*.faiss
.chroma/

# neo4j docker volume (if mounted locally)
neo4j_data/
neo4j_logs/

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.chrome-cap/
